# Statistical Method Audit Matrix

Last updated: 2026-07-16

This matrix records the current implementation and QA state for the 30 stable method IDs in the six-module registry. It is an audit artifact, not permission to make planned/disabled methods executable.

Legend: Y = covered or present, N = not present, Partial = intentionally limited coverage, N/A = not applicable because the method is not executable through that path.

| method_id | status/path | module | frontend panel | reference fixture | hand unit test | API integration | edge/failure tests | effect size / interval | CI | assumptions / warning codes | provenance | known limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `eda.descriptive` | available, generic analysis-run | exploration | `DescriptiveAnalysisPanel` | N | Y | Y | Y | N/A descriptive summary | Y | Y | Y | Numeric columns only; no inferential p-values. |
| `eda.graphical_summary` | available, generic analysis-run | exploration | `GraphicalSummaryPanel` | N | Y | Y | Y | N/A chart-data summary | Y | Y | Y | Inline chart data/UI only; no exported chart artifact. |
| `eda.normality` | available, generic analysis-run | exploration | `NormalityAnalysisPanel` | Y | Y | Y | Y | N/A diagnostic tests | Y | Y | Y | Does not automatically select downstream parametric/nonparametric methods. |
| `eda.equal_variances` | available, generic analysis-run | exploration | `EqualVariancesPanel` | Y | Y | Y | Y | N/A diagnostic tests | Y | Y | Y | Diagnostic only; does not auto-switch pooled/Welch downstream analyses. |
| `hypothesis.one_sample_t` | available, generic analysis-run | hypothesis | `OneSampleTPanel` | Y | Y | Y | Y | Y | Y | Y | Y | One-sample mean only; independence remains a design assumption. |
| `hypothesis.paired_t` | available, generic analysis-run | hypothesis | `PairedTPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Wide before/after columns only; pairing keys are not implemented. |
| `hypothesis.two_sample_t` | available, generic analysis-run | hypothesis | `TwoSampleTPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Defaults to Welch; pooled Student requires explicit option. |
| `hypothesis.one_way_anova` | available, generic analysis-run | hypothesis | `OneWayAnovaPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Standard one-way ANOVA only; Welch ANOVA/Games-Howell are not implemented. |
| `hypothesis.equivalence_tost` | available, generic analysis-run | hypothesis | `EquivalenceTostPanel` | Y | Y | Y | Y | Y | Y | Y | Y | One-sample raw-unit TOST only; paired/two-sample TOST are not implemented. |
| `hypothesis.one_sample_wilcoxon` | available, generic analysis-run | hypothesis | `OneSampleWilcoxonPanel` | Y | Y | Y | Y | Y | Y | Y | Y | One-sample signed-rank only; not labeled as a median-difference test. |
| `hypothesis.mann_whitney` | available, generic analysis-run | hypothesis | `MannWhitneyPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Two independent groups only; not a median-only interpretation. |
| `hypothesis.kruskal_wallis` | available, generic analysis-run | hypothesis | `KruskalWallisPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Dunn/Holm post-hoc only after significant omnibus result. |
| `categorical.one_proportion` | available, generic analysis-run | categorical | `OneProportionPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Binary response column only; aggregate event/trial input is not implemented. |
| `categorical.two_proportion` | available, generic analysis-run | categorical | `TwoProportionPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Binary response plus exactly two groups; zero-cell RR/OR CI is intentionally unavailable. |
| `categorical.chi_square_association` | available, generic analysis-run | categorical | `ChiSquareAssociationPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Pearson chi-square only; sparse 2x2 recommends Fisher exact but does not switch. |
| `regression.pearson` | available, generic analysis-run | regression | `PearsonCorrelationPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Pairwise Pearson correlation only; correlation is not causation. |
| `regression.xy_correlation` | available, generic analysis-run | regression | `XyCorrelationPanel` | Y | Y | Y | Y | Y | Y | Y | Y | Matrix of Pearson correlations only; no partial correlation. |
| `regression.linear_model` | available, generic analysis-run | regression | `LinearModelPanel` | Y | Y | Y | Y | Y | Y | Y | Y | OLS main effects plus selected numeric quadratic/interactions; robust covariance and categorical interactions are not implemented. |
| `regression.predict` | disabled generic page; dedicated regression-model API implemented | regression | `LinearModelPanel` prediction flow | Y (statsmodels prediction parity) | Y | Y | Y | Y | Y | Y | Y | Not executable through generic `POST /analysis-runs`; dedicated same/cross-dataset prediction has freshness, provenance, paging, CSV, and cross-artifact validation. |
| `regression.response_optimizer` | disabled generic page; dedicated RSM optimizer API implemented | regression | `ResponseOptimizerPanel` inside `ResponseSurfacePanel` | Y (NIST desirability parity) | Y | Y | Y | N/A model-based recommendation | Y | Y | Y | v0.3/config-result schema 2/source-bundle schema 2 pins the exact RSM analysis and response revision; it blocks saturated, rank-invalid, unusable-variance, significant-LOF, or dependency-invalid sources and persists explicit advisory acknowledgments. No global-optimum guarantee, uncertainty-aware desirability, nonlinear/integer constraints, or independent page. |
| `quality.attribute_control_chart` | available generic Phase I analysis; dedicated immutable limit-set API | quality | `AttributeControlChartPanel`; no Phase II selector yet | Y (Phase I NIST direct/formula parity; Phase II policy-adjusted formula foundation) | Y | Y | Y | N/A chart diagnostic | Y | Y | Y | Executable v0.1/result schema 1 remains Phase I P/NP/C/U. SQLite schema 13 implements checksum-validated limit-set schema 1 create/get/list, while v0.2/result schema 2 monitoring remains unavailable. No exact probability limits, Laney correction, or chart image export. |
| `quality.individuals_chart` | available, generic analysis-run | quality | `IndividualsChartPanel` | Y | Y | Y | Y | N/A chart diagnostic | Y | Y | Y | I/MR-style variables chart only; no chart export artifact. |
| `quality.subgroup_chart` | available, generic analysis-run | quality | `SubgroupChartPanel` | Y | Y | Y | Y | N/A chart diagnostic | Y | Y | Y | Xbar/R and Xbar/S style summaries only; no export artifact. |
| `quality.run_chart` | available, generic analysis-run | quality | `RunChartPanel` | Y | Y | Y | Y | N/A chart diagnostic | Y | Y | Y | Run-chart rules only; no control limits or process capability conclusion. |
| `quality.capability` | available, generic analysis-run | quality | `CapabilityPanel` | Partial (policy-adjusted parity) | Y | Y | Y | Y | Y | Y | Y | NIST overall-sigma summary is mapped to app field conventions; MRbar/d2 within sigma is hand-checked, not direct NIST parity. Non-normal capability and index CIs remain out of scope. |
| `quality.gage_rr` | available, generic analysis-run | quality | `GageRrPreflightPanel` | Partial (policy-adjusted parity) | Y | Y | Y | Y | Y | Y | Y | Minitab full-model summary is mapped to the app's explicit no-pooling policy; reduced-model output is not claimed as direct parity. |
| `quality.gage_run_chart` | available, generic analysis-run | quality | `GageRunChartPanel` | Y (direct deterministic payload parity) | Y | Y | Y | N/A chart diagnostic | Y | Y | Y | Synthetic ordering/redaction fixture directly matches the implemented diagnostic payload; it is not an external inferential reference. |
| `doe.factorial_design` | available through dedicated DOE design/analysis API | doe | `FactorialDesignPanel` | Y (direct NIST design/coefficient parity) | Y | Y | Y | Y where residual DF exists | Y | Y | Y | v0.3/envelope-config schema 2 pins immutable response revision schema 1; NIST `2^3` direct parity covers standard order, levels, responses, and saturated coefficients. Seeded randomization/block/checksum and reduced-model diagnostics are explicit app/hand-check conventions. |
| `doe.response_surface` | available through dedicated DOE response-surface API | doe | `ResponseSurfacePanel` | Y (direct NIST full-quadratic parity) | Y | Y | Y | Y where residual DF exists | Y | Y | Y | v0.2/envelope-config schema 2 pins immutable response revision schema 1. Design schema 2 uses generic `central_composite` family plus `alpha_mode`; legacy schema-1 family/SHA restores verbatim. No automatic term selection, Box-Behnken, blocking, report, or image export. |
| `doe.bayesian_optimization` | available through dedicated study/recommendation API | doe | `BayesianOptimizationPanel` | Y (hand EI, direct Matérn posterior algebra, five-seed Branin regret) | Y | Y | Y | N/A model-based recommendation | N/A; posterior SD is not a CI | Y | Y | Study/history schemas 1, recommendation config/result/model schemas 1, SQLite schema 12, and method v0.2.0 preserve the exact source history and GP/EI artifact relations. One pending recommendation, deterministic budgets, typed actual-unit constraint UI, confirmation-run warning, and no-global-optimum warning are enforced. Windows 11 client validation remains a release gate. |

## Verification Depth by Method

| method_id | reference / hand-check coverage | API contract coverage | edge / failure coverage | effect size / CI definition coverage | limitation visibility |
| --- | --- | --- | --- | --- | --- |
| `eda.descriptive` | Hand fixture verifies N, missing/non-numeric exclusions, mean, sample SD, quartiles, min/max. No independent external reference fixture because this is deterministic descriptive arithmetic. | `POST /api/v1/analysis-runs` with selected column IDs, stored result retrieval, row snapshot/provenance checks. Typed `DescriptiveOptions` rejects invalid column ID type, missing column IDs, and unknown option fields with `invalid_descriptive_options`. | Rejects non-dataset execution, nonnumeric/empty selections, malformed option contracts, stale/result recovery paths through shared analysis-run contracts. | N/A; explicitly no p-values or inferential effect sizes. | UI and docs label it as descriptive only. |
| `eda.graphical_summary` | Hand fixture verifies histogram bins, boxplot summary, Q-Q, ECDF chart-data payloads. No external chart renderer fixture. | Generic analysis-run endpoint plus frontend panel rendering of inline SVG chart data. Typed `GraphicalSummaryOptions` rejects invalid numeric option type, missing column IDs, and unknown option fields with `invalid_graphical_summary_options`. | Empty/non-numeric columns, point/bin caps, and malformed option contracts are tested. | N/A chart diagnostic payload; quartiles and distribution points are deterministic summaries. | UI shows chart-data diagnostics and no export artifact claim. |
| `eda.normality` | Reference fixture verifies SciPy Shapiro-Wilk, Anderson-Darling, and deterministic Q-Q values. Hand tests cover small fixtures. | Generic endpoint validates selected columns, result envelope, warnings, and no automatic downstream switch. Typed `NormalityOptions` rejects invalid numeric option type, missing column IDs, and unknown option fields with `invalid_normality_options`. | Grouping unsupported, low N, constant/non-numeric data, missing exclusions, and malformed option contracts are tested. | N/A diagnostic tests; no effect size. | Warnings and UI say normality does not select another method automatically. |
| `eda.equal_variances` | Reference fixture verifies SciPy Brown-Forsythe and Levene(mean) values; hand tests cover grouped summaries. | Generic endpoint validates response/group payload and persisted result. Typed `EqualVariancesOptions` rejects invalid numeric option type, missing required column ID, and unknown option fields with `invalid_equal_variances_options`. | Requires 2+ groups, numeric response, group sizes, nonnumeric/missing exclusions, and malformed option contracts. | N/A diagnostic tests. | Warnings state no automatic pooled/Welch or ANOVA switching. |
| `hypothesis.one_sample_t` | Reference fixture verifies SciPy t statistic/p-value; hand test verifies mean difference, SE, df, CI. | Generic endpoint validates response column, null mean, options, result retrieval. Typed `OneSampleTOptions` rejects invalid numeric option type, missing response column ID, and unknown option fields with `invalid_one_sample_t_options`. | Bad alpha/alternative, too-small N, zero SE, missing/non-numeric exclusions. | CI is one-sample t interval for mean difference; effect size is Cohen dz plus Hedges correction. | UI/docs require explicit null mean and show design assumptions. |
| `hypothesis.paired_t` | Reference fixture verifies SciPy paired t; hand test verifies `after - before`, complete-pair N, CI. | Generic endpoint validates before/after columns and stored result. Typed `PairedTOptions` rejects invalid numeric option type, missing required column ID, and unknown option fields with `invalid_paired_t_options`. | Missing/incomplete pairs, nonnumeric values, identical differences, bad options. | CI is paired mean-difference t interval; effect size is Cohen dz plus Hedges correction. | UI/docs state wide before/after only and pairing keys are out of scope. |
| `hypothesis.two_sample_t` | Reference fixture verifies Welch/default and pooled explicit cases; hand tests verify group summaries and CI. | Generic endpoint validates response/group roles, variance option, result envelope. Typed `TwoSampleTOptions` rejects invalid numeric option type, missing response column ID, and unknown option fields with `invalid_two_sample_t_options`. | Exactly-two-group requirement, small groups, zero variance, missing/non-numeric exclusions. | CI is two-sample mean-difference interval; Cohen's d and Hedges g are reported. | UI/docs state Welch default and pooled requires explicit option. |
| `hypothesis.one_way_anova` | Reference fixture verifies SciPy `f_oneway`, ANOVA table, eta/omega squared, and Tukey-Kramer values; hand tests verify sums of squares. | Generic endpoint validates response/group options and persisted result. Typed `OneWayAnovaOptions` rejects invalid numeric option type, missing required column ID, and unknown option fields with `invalid_one_way_anova_options`. | Tests now cover significant-only posthoc, non-significant skip, negative omega squared retained from formula, group-size imbalance warning, invalid ANOVA/posthoc options, low N, identical/zero residual variance. | Eta squared is `SS_between/SS_total`; omega squared is `(SS_between-df_between*MSE)/(SS_total+MSE)` and may be negative rather than clamped; Tukey-Kramer CIs use studentized range. | UI/docs state standard ANOVA only; Welch/Games-Howell are not implemented. |
| `hypothesis.equivalence_tost` | Reference fixture verifies SciPy-backed lower/upper one-sided t values; hand tests verify TOST decision and CI. | Generic endpoint validates one-sample payload; typed `EquivalenceTostOptions` adapter rejects invalid type, missing required bound, and unknown fields with `invalid_equivalence_tost_options`. | Tests cover bound order, one-sided-only pass not equivalent, ordinary t-test non-significance not equivalent, zero SE, low N, missing/non-numeric exclusions. | CI level is `1 - 2 * alpha`; equivalence requires both one-sided tests to reject; effect size is Cohen dz plus Hedges correction for mean-minus-reference. | UI/docs warn that non-significance is not equivalence and bounds must be user-defined. |
| `hypothesis.one_sample_wilcoxon` | Reference fixture verifies SciPy signed-rank exact/asymptotic values; hand test verifies signed ranks. | Generic endpoint validates response and null location. Typed `OneSampleWilcoxonOptions` rejects invalid numeric option type, missing response column ID, and unknown option fields with `invalid_one_sample_wilcoxon_options`. | Zero differences, ties, low N, missing/non-numeric exclusions, invalid alternatives. | Effect size is rank-biserial for signed ranks; Hodges-Lehmann CI is not implemented. | UI/docs avoid describing it as an automatic median-difference proof. |
| `hypothesis.mann_whitney` | Reference fixture verifies SciPy U/p-values; hand test verifies rank sums and common-language probability. | Generic endpoint validates response/group roles. Typed `MannWhitneyOptions` rejects invalid alpha type, missing group column ID, and unknown option fields with `invalid_mann_whitney_options`. | Exactly-two-group requirement, ties/exact warning, low N, missing/non-numeric exclusions. | Rank-biserial and common-language probability are reported; no median-difference CI. | UI/docs state independent two-group rank comparison only. |
| `hypothesis.kruskal_wallis` | Reference fixture verifies SciPy H/p-value, tie correction, epsilon squared, Dunn/Holm posthoc values. | Generic endpoint validates grouped response payload. Typed `KruskalWallisOptions` rejects invalid alpha type, missing required column ID, and unknown option fields with `invalid_kruskal_wallis_options`. | 3+ group requirement, posthoc only after significant omnibus, tie/low-N warnings, missing/non-numeric exclusions. | Epsilon squared is reported; Dunn posthoc p-values use Holm adjustment. | UI/docs state no automatic ANOVA/nonparametric switching. |
| `categorical.one_proportion` | Reference fixture verifies exact binomial p-value and Wilson/Clopper-Pearson intervals; hand test verifies event counts. | Generic endpoint validates event-level options and result retrieval. Typed `OneProportionOptions` rejects invalid numeric option type, missing event level, and unknown option fields with `invalid_one_proportion_options`. | Binary-level requirement, event missing, invalid CI method, no event/all event edge cases. | CI is Wilson or Clopper-Pearson; effect size is Cohen h. | UI/docs state only binary column input, not event/trial summary input. |
| `categorical.two_proportion` | Reference fixture verifies Fisher exact p-value, Newcombe-Wilson difference CI, risk ratio, odds ratio where finite; hand tests verify 2x2 counts. | Generic endpoint validates response/group/event-level payload. Typed `TwoProportionOptions` rejects invalid numeric option type, missing event level, and unknown option fields with `invalid_two_proportion_options`. | Tests now cover zero-cell RR/OR unavailable, all-event/no-event groups, exactly-two-group requirement, binary response requirement, missing groups/responses. | Difference CI is Newcombe-Wilson; RR/OR use log-Wald CI only when finite and otherwise return null with warning. | UI/docs state summary event/trial input is out of scope and zero-cell effects may be unavailable. |
| `categorical.chi_square_association` | Reference fixture verifies SciPy chi-square statistic/p-value, expected counts, and Cramer's V; hand test verifies residual formula. | Generic endpoint validates two categorical columns and persisted result. Typed `ChiSquareAssociationOptions` rejects invalid alpha type, missing required row/column IDs, and unknown option fields with `invalid_chi_square_options`. | Tests cover sparse 2x2 expected-count diagnostics, Fisher recommendation without automatic fallback, finite standardized residuals, low level counts, missing exclusions. | Cramer's V is reported; no Fisher exact p-value is fabricated. | UI/docs show Fisher recommendation as guidance only. |
| `regression.pearson` | Reference fixture verifies Pearson r/p-value and Fisher z CI; hand test verifies covariance/r. | Generic endpoint validates two numeric columns, result retrieval, and provenance. Typed `PearsonOptions` rejects invalid numeric option type, missing X column ID, and unknown option fields with `invalid_pearson_options`. | Constant columns, low N, missing/non-numeric exclusions, malformed option contracts, non-causation warnings. | CI is Fisher z interval for Pearson r; r-squared is reported. | UI/docs warn correlation is not causation. |
| `regression.xy_correlation` | Reference fixture verifies pairwise Pearson matrix values and per-cell failure codes; hand tests cover small matrix. | Generic endpoint validates selected X/Y columns and no fake statistics for failed cells. Typed `XyCorrelationOptions` rejects invalid numeric option type, missing Y column IDs, and unknown option fields with `invalid_xy_correlation_options`. | Non-numeric, low N, constant-pair cells, missing exclusions, matrix cell failures, malformed option contracts. | Fisher z CI and r-squared per valid pair. | UI/docs state Pearson matrix only; no partial/Spearman/Kendall. |
| `regression.linear_model` | `regression_linear_model_reference.json` plus its synthetic CSV independently cross-checks statsmodels 0.14.6 treatment-coded OLS fit, coefficients, SE, t/p, CI, residual sigma, R-squared/F test, VIF/condition number, and three mean/CI/PI predictions. Existing NumPy and hand fixtures retain diagnostic and selected-term coverage. | Generic endpoint stores result plus checksum-validated app-created model manifest; dedicated regression prediction endpoints validate manifest and prediction payloads. Typed `LinearModelOptions` rejects invalid option type, missing predictor IDs, unknown top-level fields, and unknown nested interaction-term fields with `invalid_linear_model_options`. | Tests cover rank-deficient/singular design, constant predictors/response, single-level factors, categorical treatment coding, unseen categorical prediction preflight, numeric extrapolation warning, finite prediction intervals, and missing prediction basis. | Coefficient CIs use OLS t intervals; prediction API returns statsmodels-cross-checked mean-response CI and individual prediction interval from the stored OLS basis. | UI/docs state OLS main effects plus selected numeric terms only; generic `regression.predict` remains disabled and regression is not causal. |
| `regression.predict` | The statsmodels 0.14.6 synthetic linear-model fixture directly cross-checks three predicted means, mean-response CIs, and individual PIs; hand/API fixtures cover row counts and exclusions. | Dedicated preflight/prediction, stored-result restore, paged rows, full CSV, export list/download, and OpenAPI provenance contracts. | Source stale/schema/method drift and target incompatibility are distinct; config/result/rows/model IDs, versions, counts, checksums, target version, and manifest SHA tampering are rejected without path/value disclosure. | Prediction intervals come from the stored OLS covariance basis and match the independent statsmodels fixture. | UI/docs point to the Linear Model panel; generic Predict page remains disabled while dedicated prediction is implemented. |
| `regression.response_optimizer` | `response_optimizer_nist_reference.json` reproduces the NIST/SEMATECH four-response Derringer-Suich example with published rounded fitted equations, best coded point, individual desirabilities, and composite desirability; hand tests cover all four goals and weighted geometric composition. | Dedicated response-surface optimization create/restore routes validate source RSM result/response/design checksums, source bundle, config/result relationship, method version, runtime/build provenance, and typed OpenAPI/frontend contracts; generic analysis-run remains disabled. | Invalid goal thresholds, out-of-region bounds, invalid/infeasible linear constraints, duplicate/missing/invalid sources, evaluation budget termination, config/result/checksum tamper, and path non-exposure are tested. | Individual desirability and importance-weighted geometric composite are reported from point predictions; prediction uncertainty is not incorporated. | RSM panel exposes one current response objective plus bounds, one optional linear constraint, and deterministic budgets. Backend supports up to eight objectives sharing one factor space. Every result denies global-optimum guarantee and requires model review plus confirmation runs. |
| `quality.attribute_control_chart` | `quality_attribute_control_chart_nist_reference.json` directly verifies the published NIST C-chart example and P proportions, derives fixed-n NP parity, and independently evaluates NIST U formulas; hand tests cover weighted P limits. The Phase II policy fixture independently checks frozen P/NP/C/U formulas and is explicitly not direct published-output parity or an executable monitoring result. | Generic endpoint validates the closed Phase I option contract and common persistence/exports. Dedicated create/get/list routes expose immutable app-created limit sets with typed frontend/OpenAPI contracts; the current analysis request still rejects `phase` and `limit_set_id`. | In addition to Phase I failures, tests cover schema-12 upgrade, P/NP/C/U promotion, minimum-point/approximation/dispersion/signal eligibility, idempotence, no overwrite/delete, file/metadata/rehashed-asset/source-result/row-snapshot/path tamper, and public error redaction. | N/A chart diagnostic; Phase I point limits and Pearson dispersion are independently recomputed before baseline promotion. | UI states that the current screen estimates Phase I limits and does not apply stored Phase II limits. Limit-set API types exist, but selection and monitoring execution UI do not. |
| `quality.individuals_chart` | Reference/hand coverage verifies I/MR calculations and chart payload. | Generic endpoint validates numeric response/order options. Typed `IndividualsChartOptions` rejects invalid integer option type, missing value column ID, and unknown option fields with `invalid_individuals_chart_options`. | Missing/non-numeric exclusions, order handling, point caps, invalid options. | N/A chart diagnostic; control-limit statistics are reported, not effect sizes. | UI/docs state no chart export artifact. |
| `quality.subgroup_chart` | Reference/hand coverage verifies Xbar/R and Xbar/S summaries. | Generic endpoint validates measurement/subgroup roles. Typed `SubgroupChartOptions` rejects invalid point-limit type, missing subgroup column ID, and unknown option fields with `invalid_subgroup_chart_options`. | Unbalanced subgroup handling, small subgroup constraints, missing/non-numeric exclusions, malformed option contracts. | N/A chart diagnostic. | UI/docs state variables subgroup charts only. |
| `quality.run_chart` | Reference/hand coverage verifies run-chart points and pattern summaries. | Generic endpoint validates response/order options. Typed `RunChartOptions` rejects invalid numeric option type, missing value column ID, invalid point-limit type, and unknown option fields with `invalid_run_chart_options`. | Missing/non-numeric exclusions, ordering, point caps, invalid options. | N/A diagnostic chart. | UI/docs state no control-limit or capability conclusion. |
| `quality.capability` | Partial: hand fixtures verify two-sided target and one-sided/exclusion cases. `quality_capability_normal_reference.json` independently cross-checks the overall sample-SD indices against the published NIST Cp/Cpk/Cpl/Cpu summary example, while the application-specific moving-range within sigma remains a separate hand-check. No raw-data industrial software fixture yet. | Generic endpoint validates value column and spec options. Typed `CapabilityOptions` rejects invalid spec-limit type, missing value column ID, invalid histogram-bin type, and unknown option fields with `invalid_capability_options`. | Tests cover one-sided spec, target outside spec, zero overall sigma, unsupported missing policy, invalid histogram limit; zero within sigma guard exists but is not independently reachable with moving-range length 2 before zero-overall catches all-identical data. | Capability indices use normal overall sigma and moving-range within sigma point estimates; index CIs and non-normal capability are not implemented. | UI/docs show normal-only, point-estimate-only limitations and stability/measurement-system warnings. |
| `quality.gage_rr` | Partial: hand fixtures verify balanced crossed ANOVA sums of squares, SciPy F-tail p-values, variance components, negative raw component clamping, ndc, warnings, and label redaction. `quality_gage_rr_crossed_reference.json` independently cross-checks the official Minitab full-model interaction ANOVA summary and derives the application's no-pooling components from its published rounded mean squares. Minitab's reduced-model final table is not a direct parity target. | Generic endpoint validates Gage R&R roles and preflight/execution flow. Typed `GageRrOptions` rejects invalid column ID type, missing required column ID, and unknown option fields with `invalid_gage_rr_options`. | Tests cover unbalanced crossed design rejection, zero total variation rejection, negative raw/final variance policy, duplicate/missing identifiers through preflight contracts. | Variance components and percent contribution/study variation are reported; negative raw estimates are preserved and final variances clamp to zero. | UI/docs state balanced crossed ANOVA only; automatic interaction pooling, advanced variants, and plots are out of scope. |
| `quality.gage_run_chart` | Reference/hand coverage now includes `quality_gage_run_chart_ordering_reference.json`, which verifies balanced summaries, tied order-column sorting with canonical-position tie-breaking, every displayed point index, exact exclusions/warnings, truncation metadata, and serialized raw-label redaction. No external package fixture is required because this is deterministic diagnostic chart data rather than an inferential statistic. | Generic endpoint validates measurement/part/operator/replicate roles, row-snapshot provenance, persisted result retrieval, and result rendering. Typed `GageRunChartOptions` rejects invalid column ID type, missing required column ID, invalid point-limit type, and unknown option fields with `invalid_gage_run_chart_options`. | Tests cover duplicate replicates, unbalanced design, too-small replicate count, canonical order fallback, missing/invalid input exclusions, point truncation, and malformed option contracts. | N/A diagnostic chart. | UI/docs state it is diagnostic only, inline truncation does not change full-sample summaries, and it does not replace variance-component Gage R&R. |
| `doe.factorial_design` | `doe_factorial_design_reference.json` cross-checks official NIST `2^3` coded/actual standard order. `doe_factorial_analysis_nist_reference.json` directly checks the published eight responses and all saturated coefficients; effects are twice coefficients. Hand tests cover hierarchy and reduced-model lack of fit. | Dedicated design, immutable response-revision current/history/correction, analysis create/restore, and HTML report routes cover schema-v10 storage, response revision SHA dependencies, old-analysis restore, runtime/build provenance, and typed frontend/OpenAPI contracts; generic analysis-run still returns `analysis_method_uses_dedicated_api`. | Tests cover invalid design/coding/response, revision run-set/checksum/relation tamper, rank/constant failures, saturated inference nulls, result/config/dependency tamper, report restore, and internal-path non-exposure. | Coefficients/effects and 95% CI/p-values are returned when residual DF exists; saturated designs explicitly return no fake inference. Pure error/lack-of-fit is reported only when estimable. | UI/docs distinguish current/history revisions, require explicit correction, and preserve analyzed revisions as read-only. Fractional alias analysis and optimization remain separate work. |
| `doe.response_surface` | `doe_response_surface_nist_reference.json` directly checks the published 11-run NIST CCI Uniformity full-quadratic coefficients, residual SE, R-squared, adjusted R-squared, F, and p-value. Hand tests recover a known stationary maximum and rotatable/face-centered geometry. | Dedicated design, immutable response-revision current/history/correction, and analysis create/restore routes cover schema-v10 storage, design/result/revision checksums, exact historical revision dependencies, common runtime/build provenance, and typed frontend/OpenAPI contracts; generic analysis-run returns `analysis_method_uses_dedicated_api`. | Duplicate/range/center errors, revision run-set/checksum/relation tamper, constant response, rank deficiency, invalid contour grid, config dependency tamper, and path non-exposure are tested. | Coefficient 95% CI/p-values are returned when residual variance/DF exist. Pure error/lack-of-fit is reported only when estimable; no fake inference is returned for zero residual variance. | UI/docs distinguish current/history revisions, require explicit correction, and preserve analyzed revisions as read-only. The stationary point is not presented as an optimizer recommendation. |
| `doe.bayesian_optimization` | Policy fixture verifies hand EI values, documented quadratic/Branin optima, and five seeded 20-trial Branin sequences with maximum simple regret `0.20` and median `0.15` gates. Calculation tests independently reconstruct Matérn-5/2 covariance, posterior mean, and posterior variance from persisted fitted parameters. | Catalog is available, generic analysis-run directs to the dedicated API, and study/trial/history/recommendation create/list/restore routes are typed. The UI creates constrained or unconstrained studies, records observations, and renders pending recommendations and constraint feasibility separately from observations. | Tests cover metadata/constraint errors, deterministic seed parity, stale history, one pending recommendation, bounds, minimize/maximize direction, no-feasible and fit-budget errors, schema-11 migration, definition/trial/history/recommendation tamper, rehashed coordinate/budget/model/incumbent/constraint/source-history/provenance relationship tamper, and path/value redaction. | Result separates predicted mean, posterior SD, EI, incumbent, constraints, model, and budgets. Posterior SD is explicitly not a confidence interval. | Korean guidance states that the app does not execute objectives, recommendations require confirmation, and no global optimum is guaranteed. |

## Current Audit Notes

- The catalog contains 30 stable method IDs: 28 are catalog-available and two
  are disabled. Of the 28 available IDs, 25 execute through the generic
  `MethodExecutionHandler` registry. `doe.factorial_design` and
  `doe.response_surface` use dedicated DOE design routes, and
  `doe.bayesian_optimization` uses the dedicated study/recommendation routes.
- Generic analysis-run results now include runtime/build provenance fields: `python_version`, `platform`, `build_commit`, and `package_versions`; these fields are built through a shared analysis-run provenance helper. The helper now prefers `Settings.git_commit` and falls back to `DATALAB_GIT_COMMIT`.
- Method versions now have a single `METHOD_VERSIONS` map covering all 30 stable method IDs. The method catalog and generic execution handler specs read from this source, and API contract tests assert catalog/handler alignment.
- Method version operating rules are documented in `docs/method_versioning.md`.
  `regression.predict` is `0.2.0` because persisted dependency provenance and
  artifact relationship fields changed. `doe.factorial_design` is `0.3.0`
  and `doe.response_surface` is `0.2.0` because analysis envelope/config schema
  2 now pins immutable response revision schema 1. `regression.response_optimizer`
  is `0.3.0` because source-bundle schema 2 pins the source analysis revision;
  optimizer config/result schemas remain 2. SQLite schema 10 stores revision
  heads, ordered values, and analysis relationships without rewriting legacy
  result artifacts. `doe.bayesian_optimization` is `0.2.0`; its unchanged
  study/history schemas remain 1, recommendation config/result/model schemas
  start at 1, and SQLite schema 12 stores recommendation relationships while
  preserving legacy `0.1.0` studies.
- All four EDA methods, all eight hypothesis methods, all three categorical methods, all three generic regression analysis-run methods, and all six current quality analysis-run methods now have typed option adapters at the runner boundary. They reject malformed option contracts before row snapshots or result artifacts are written, and tests assert sanitized error responses.
- EDA, categorical, hypothesis, quality, and simple generic regression analysis-run methods now persist successful result envelopes through the shared `store_succeeded_analysis_result` helper, including result checksum calculation and result-file cleanup if metadata insertion fails. `regression.linear_model` uses a regression-specific manifest-aware persistence wrapper for the result envelope plus `regression_model_manifest` artifact.
- API contract tests now check that result-only runner modules do not regain direct low-level metadata insert or file-write primitives, and that `regression.linear_model` remains on the manifest-aware persistence boundary.
- `eda.descriptive`, `eda.graphical_summary`, `eda.normality`, `eda.equal_variances`, all eight hypothesis methods, all three categorical methods, the three generic regression analysis-run methods, and all six current quality analysis-run methods now execute through the `MethodExecutionHandler` registry foundation. Handler metadata and missing-runner validation are centralized in `services/analysis_method_handlers.py`; the four EDA runners are split into `services/analysis_runners_eda.py`; the eight hypothesis runners are split into `services/analysis_runners_hypothesis.py`; the three categorical runners are split into `services/analysis_runners_categorical.py`; the three generic regression runners, including `regression.linear_model` plus its safe JSON model-manifest persistence, are split into `services/analysis_runners_regression.py`; and the six current quality runners are split into `services/analysis_runners_quality.py`.
- `build_commit` is read from `Settings.git_commit` first, then `DATALAB_GIT_COMMIT`; otherwise it may be null.
- `regression.predict` remains disabled only for the generic method page. Its
  working execution path is the Linear Model panel and dedicated regression-model
  APIs, with source freshness and result/config/rows/model consistency checks.
- `doe.factorial_design` is registry-available but uses dedicated DOE design, response, analysis, and report routes, not the generic analysis-run endpoint.
- Methods marked planned/disabled must continue to reject without result envelopes or mock statistics.
- Workbench saved-result history, export, comparison, and restore requests use
  latest-request guards. Resetting dataset/result context or unmounting the
  owning component invalidates in-flight work, and an older response or
  `finally` block cannot overwrite the latest state or clear its loading
  indicator. `AnalysisShell` and
  `AnalysisWorkbench` accept these concerns only through four grouped state
  props; the duplicate field-by-field fallback contract has been removed.
- The highest-risk statistical regression tests currently emphasize ANOVA post-hoc/effect sizes, TOST one-sided logic, zero-cell categorical effects, expected-count diagnostics, OLS singular/rank cases, capability zero-sigma/one-sided specs, and balanced crossed Gage R&R assumptions.
- Quality/DOE reference coverage was strengthened for `quality.capability`, `quality.gage_rr`, `quality.gage_run_chart`, and `doe.factorial_design`. DOE now has direct official NIST parity for both standard order and saturated coefficients, while seeded/block/checksum behavior and reduced-model diagnostics remain explicit application/hand-check conventions.

## Independent Reference Backlog

These methods are available or product-visible, but their independent reference
coverage is still partial. The first `quality.capability` NIST summary fixture
and `quality.gage_rr` Minitab full-model summary fixture are now present, and
the synthetic `quality.gage_run_chart` ordering fixture now completes its
approved diagnostic-payload scope. Compatible raw-data software cross-checks
and the statsmodels `regression.linear_model` fit/prediction fixture is now
present. Official NIST `doe.factorial_design` fixtures now cover design order
and saturated coefficients. Compatible raw-data capability/Gage R&R
cross-checks remain backlog; add them before changing those formulas, result
schemas, defaults, or method versions.

Fixture acceptance rules:

- Use synthetic, public, or internally hand-authored data only.
- Record source, tool/version, options, rounding policy, and validation date.
- Store full-precision expected values where available; round only for display
  assertions.
- Assert warnings, exclusions, group sizes, method metadata, and provenance, not
  only numeric fields.
- Include at least one edge or failure case next to the reference happy path.
- Do not introduce a production dependency or method-version bump in the
  fixture PR.

| method_id | first fixture file to add | expected output source | tolerance | fields to verify | license/source check before implementation |
| --- | --- | --- | --- | --- | --- |
| `quality.capability` | Added: `backend/tests/reference/fixtures/quality_capability_normal_reference.json`. Remaining: a compact public/raw-data industrial-software fixture if a compatible sigma convention is found. | Official NIST/SEMATECH section 6.1.6 summary example: LSL 8, USL 20, mean 16, sample SD 2, Cp 1.0, Cpk/Cpu 0.6667, and Cpl 1.3333. Synthetic rows reproduce the published summary; they are not copied source data. | Exact N/exclusion/warning checks, `1e-12` for synthetic summary and moving-range hand checks, and `1e-4` for NIST values published to four decimals. | Verified now: `n_total`, `n_used`, exclusions, sample SD, moving-range within sigma, LSL/USL metadata, Cp/Cpk/Cpl/Cpu mapped to application Pp/Ppk-style overall fields, and warning codes. Existing API tests retain method ID/version and row snapshot provenance coverage. Remaining independent coverage: raw-data expected nonconformance/ppm and industrial within-sigma convention. | Confirmed official United States government technical source; only formulas and a small published numerical summary are recorded. No proprietary table, screenshot, or raw dataset is copied. The fixture records the sample-SD versus MRbar/d2 convention difference and does not claim process stability or adequate capability-study sample size. |
| `quality.gage_rr` | Added: `backend/tests/reference/fixtures/quality_gage_rr_crossed_reference.json`. Remaining: a redistributable raw-data crossed fixture with an explicitly compatible no-pooling policy. | Official Minitab Crossed Gage R&R example and formula pages. The fixture records the published 10-part, 3-operator, 3-replicate full-model ANOVA table and the separately published reduced-model variance table. | `1e-12` for components derived from published rounded mean squares, `0.1` for rounded F, `0.001` for rounded interaction p-value, and exact design count, ndc, policy, clamp, and warning checks. | Verified now: balanced part/operator/replicate counts, full-model interaction ANOVA row, raw and final variance components under the app's no-pooling policy, negative raw component handling, percent contribution, percent study variation, ndc, and warnings. Existing API tests retain redacted label behavior, method ID/version, and provenance coverage. Remaining independent coverage: raw-row ANOVA parity under an identical pooling policy. | Confirmed official copyrighted vendor source; only a small numerical summary and links are recorded. No worksheet, screenshot, prose passage, or raw measurements are copied. The fixture explicitly records that Minitab removes the nonsignificant interaction while the app preserves it, so reduced-model values are not asserted as parity. |
| `quality.gage_run_chart` | Added: `backend/tests/reference/fixtures/quality_gage_run_chart_ordering_reference.json`. | Internal hand-reviewed synthetic diagnostic fixture because current output is deterministic chart-data ordering, not an inferential statistic; no public chart screenshot is stored or asserted. | Exact equality for point ordering, redacted labels, indexes, truncation flags, exclusion counts, and warning codes; `1e-12` for plotted measurements and summaries. | Verified now: balanced-design warnings, order-column tie behavior, canonical-position tie-break, point cap/truncation metadata, redacted labels across the serialized result, every displayed point index, N/exclusions, and duplicate-replicate failure. Existing API tests retain method ID/version, row-snapshot provenance, persistence, and retrieval coverage. | Verified that all labels and rows are synthetic and do not encode real operator or part IDs. No external dataset, proprietary output, or chart screenshot artifact is included. |
| `doe.factorial_design` | Added: `backend/tests/reference/fixtures/doe_factorial_design_reference.json` and `doe_factorial_analysis_nist_reference.json`. | Official NIST/SEMATECH `2^3` standard order/factor-setting tables plus the published eight-response factorial effects example. Seeded shuffle, center-point, block, checksum, and reduced-model lack-of-fit behavior remain explicit application/hand-check contracts. | Exact equality for coded order/metadata; `1e-12` for actual levels, response values, coefficients, effect scaling, hierarchy, and hand-check reduced-model partitions. | Verified now: factor metadata/order, published responses, intercept and all seven saturated coefficients, `effect=2*coefficient`, saturated null inference, hierarchy, lack-of-fit, design/result/response checksums, dedicated API persistence/provenance/tamper, and frontend/report rendering. Remaining: fractional alias analysis and optimization. | Confirmed official United States government technical source. Only small published numerical tables and links are recorded; no screenshot or prose passage is copied. The fixture does not attribute app randomization, checksum, storage, or reduced-model policies to NIST. |
| `doe.response_surface` | Added: `backend/tests/reference/fixtures/doe_response_surface_nist_reference.json`. | Official NIST/SEMATECH 11-run central composite inscribed Uniformity example and its published full quadratic OLS output. The NIST CCD page independently defines rotatable `alpha=(2^k)^(1/4)`. | `1e-5` for coefficients published to five decimals and `5e-4` for fit statistics published to three/four decimals; `1e-12` hand tolerances for bounded geometry and known quadratic stationary point. | Verified now: CCI/face-centered point geometry, actual bound mapping, all full-quadratic coefficients, residual SE, R-squared/adjusted R-squared, F/p, known maximum classification/coordinates, contour count, rank/constant failures, design/result/response checksums, restore/tamper, and frontend rendering. | Confirmed official United States government technical source. Only the small published coded/response table, numerical fit summary, and links are recorded. Application bounded CCI scaling, storage, randomization, contour, and stationary policies are not attributed to NIST. |
| `doe.bayesian_optimization` | `backend/tests/reference/fixtures/doe_bayesian_optimization_reference_policy.json`; hand-recomputed definition/trial/history SHA, direct Matérn posterior algebra, and a five-seed sequential Branin reference are included. | Jones/Schonlau/Welch EGO and Rasmussen/Williams GP references, scikit-learn GP semantics, BoTorch Branin formula/minima, plus hand EI/quadratic cases. SHA-256 counter initial points are an explicit application policy. | `1e-12` for analytic EI/determinism, `1e-9` for direct posterior algebra, `1e-12` restore normalization tolerance, exact SHA equality; five seeds use a declared 20-trial budget with max regret `0.20` and median regret `0.15`. | Verified now: method/version, deterministic studies, immutable history, EI/posterior arithmetic, both directions, constraints and UI, bounded acquisition, recommendation persistence/provenance, pending rule, rehashed relationship tamper/redaction, local timing characterization, no objective execution, and no-global-optimum warning. Remaining: Windows 11 release validation and characterization across additional benchmark families/budgets. | Sources are public method/library documentation; only formulas, small analytic constants, links, thresholds, and metadata are stored. scikit-learn is pinned; PyTorch and BoTorch are not product dependencies. |
| `regression.linear_model` | Added: `backend/tests/reference/fixtures/regression_linear_model_reference.json` plus `regression_linear_model_reference.csv`. | statsmodels 0.14.6 OLS formula output from fully synthetic data with explicit intercept, treatment coding/reference `A`, standard covariance, 95% coefficient intervals, and `get_prediction` rows. | `1e-8` for coefficients, SE, t, p, CI, residual sigma, R-squared/adjusted R-squared, F statistic, prediction mean, mean CI, prediction interval, VIF, and condition number; exact equality for df, mapped term names, reference levels, warning codes, and CSV SHA-256. | Verified now: design matrix term order mapping, treatment coding, categorical reference level, coefficients, df, sigma, R-squared, adjusted R-squared, F test, coefficient CIs, prediction mean/CI/PI rows, VIF/condition diagnostics, warnings, and single-level failure. Existing API tests retain method ID/version, model manifest checksum equality/tamper recovery, and row-snapshot provenance. | Confirmed statsmodels Modified BSD (3-clause) license and recorded generator package versions/options. The one-time tool remained outside repository dependencies, the CSV is synthetic, and no external model artifact, pickle, or joblib is loaded. |
