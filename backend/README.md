# DataLab Studio Backend

FastAPI backend package for the local-first DataLab Studio application.

The public API is versioned under `/api/v1`. The initial Gate A surface exposes `/api/v1/health` only.
